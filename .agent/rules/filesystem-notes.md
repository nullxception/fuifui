# File system notes

- `models/` subdirectory contains \*.safetensor model files (may be .gitignored)
- `public/upload/` contains user uploaded files (may be .gitignored)
- `output/` directory contains generated images by stable-diffusion.cpp (may be .gitignored)
- These directories may not be visible to AI agents due to .gitignore rules
